{
  "name": "homebase",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -e json,mbtiles -x \"npm run server\"",
    "server": "tileserver-gl-light -c tileserver.config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^1.11.0",
    "tileserver-gl-light": "^1.7.0"
  }
}
